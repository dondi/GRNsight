// var assert = require("chai").assert;
// import { grnState } from "./grnstate";

/* TODO: Framework for front-end test

var testResetNormalization = (input, propertyExpected) => {
    assert.equal(propertyExpected, grnState.propertyValue);
}

describe("The grnState model object", () => {
    it("updates the reset normalization maximum correctly", () => {
        grnState.network = {
            Build a network here. Because reset normalization max only relies on positiveWeights and negativeWeights,
            your test network may contain just those properties.
        };

        grnState.resetNormalizationMax.should.equal(whatever it should be);
    });

    Provide additional tests as needed.
});
*/
